name: CodexDock
options:
  bundleIdPrefix: com.aelaguiz
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    SWIFT_VERSION: "6.0"
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
targets:
  CodexDock:
    type: framework
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: CodexDock
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.aelaguiz.CodexDock
  CodexDockApp:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: CodexDockApp
    dependencies:
      - target: CodexDock
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.aelaguiz.CodexDockApp
        ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS: NO
    info:
      path: CodexDockApp/Info.plist
      properties:
        CFBundleDisplayName: Codex Dock
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        UILaunchScreen: {}
        NSLocalNetworkUsageDescription: Connect to Codex app-server hosts on your private network.
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
  CodexDockTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: CodexDockTests
    dependencies:
      - target: CodexDock
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
schemes:
  CodexDockApp:
    build:
      targets:
        CodexDockApp: all
        CodexDockTests: [test]
    test:
      targets:
        - CodexDockTests
      gatherCoverageData: false
