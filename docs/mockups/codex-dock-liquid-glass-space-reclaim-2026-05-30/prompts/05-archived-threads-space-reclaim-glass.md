Create a polished iPhone 17 portrait UI mockup for Codex Dock.
Use the attached `base-04-archived-threads-sheet.png` as the primary layout
source. This is a Liquid Glass refinement of the reduced-space Archived Threads
direction.

Product direction:
- Archived Threads is a sheet opened from More or Archive Cleanup.
- It is not a permanent bottom tab.
- The old bottom `Dock` / `Archive` / `Relay` tab bar is gone.
- Search and compact filters support recovery without taking daily Dock space.

Screen state:
- Background Dock screen has no bottom tab bar.
- Inset sheet title: "Archived Threads".
- Summary metrics: "2,138 archived" and "Across 2 hosts".
- Search field: "Search archived threads".
- Compact filters: "All hosts", "Amir-M5", "Home", "Last archived".
- Select mode visible with "Select", "Restore selected", and "Restore 3".
- Archived rows grouped by "Today", "This week", and "April 2026".

Exact readable text to include:
- "Archived Threads"
- "2,138 archived"
- "Across 2 hosts"
- "Search archived threads"
- "All hosts"
- "Amir-M5"
- "Home"
- "Last archived"
- "Select"
- "Restore selected"
- "Today"
- "This week"
- "April 2026"
- "3 selected"
- "Restore 3"

Visual treatment:
- Use Liquid Glass for the sheet, search field, compact filters, and bottom
  restore action.
- Keep archived rows solid and readable.
- No bottom tab bar. No permanent Archive root destination.
- No decorative background effects.
