Create a polished iPhone 17 portrait UI mockup for Codex Dock showing a live
Thread detail state with dictation active and a request card visible. Use the
attached legacy Session detail mockup and compact controls reference as anchors.

Design goal:
- Explore the latest iOS 26 look for the high-interaction detail surface:
  voice dictation, request response, and send controls.
- Make the app feel native, precise, and operational, not like a generic chat app.
- Use Liquid Glass only where it helps interaction: toolbar actions, live voice
  status, composer, and primary request action.

Layout:
- Native iPhone 17 screenshot, Dynamic Island, 9:41, no frame.
- Standard inline toolbar with "Dock" back control, title "Thread", and More.
- Header card is compact, not oversized. Show title
  "Review archive cleanup plan" with metadata chips.
- Show a live status strip on glass: "Listening. Tap stop to finalize." with a
  waveform/mic icon and a small "Stop" button.
- Show request card with clear choice actions:
  "Approval needed", "Run command in workspace", command text, "Deny",
  "Approve".
- Composer at bottom uses Liquid Glass and contains drafted text.

Exact readable UI text to include:
- "Dock"
- "Thread"
- "Review archive cleanup plan"
- "Amir-M5"
- "codex-client"
- "archive-cleanup"
- "Live"
- "Listening. Tap stop to finalize."
- "Stop"
- "Approval needed"
- "Run command in workspace"
- "rtk make relay-doctor"
- "Deny"
- "Approve"
- "Message Codex"
- "Summarize the failing route first."
- "Hold"
- "Send"

Visual rules:
- The active voice state should feel alive but restrained; no decorative sound
  wave hero.
- Request card content must stay solid and readable. Only the primary action can
  use prominent glass/color.
- Avoid fake chat bubbles that erase the current Codex Dock timeline style.
- Text must be sharp and readable.
