# Participants

- Model A: raw `Opus 4 8 Max` -> `runtime=claude`, `model=claude-opus-4-8`, `effort=max`, `role=collaborator`
- Model B: raw `GBD-55XI` -> interpreted as prior shorthand `GPT-5.5 xhigh` -> `runtime=codex`, `model=gpt-5.5`, `effort=xhigh`, `role=collaborator`

`codex debug models` confirmed `gpt-5.5` supports `xhigh`.
